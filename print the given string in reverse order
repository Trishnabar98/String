#include <stdio.h>
#include <string.h>
#define MAX 9999

int main(void) {
	
	char s[MAX];
    int i,len=0,f=0;
    gets(s);
    len=strlen(s);
    for(i=len-1;i>=0;i--)
    {
       printf("%c",s[i]);
    }
    
	return 0;
}

