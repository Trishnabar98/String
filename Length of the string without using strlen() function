#include <stdio.h>
#include <string.h>

int main(void) {
	// your code goes here
	char s[100];
    int i,l=0;
    gets(s);
    for(i=0;s[i]!='\0';i++)
    {
        l++;
    }
    printf("%d",l);
	return 0;
}

