#include <stdio.h>

int main(void) {
	// your code goes here
	char s[100];
	int i,sum=0;
	gets(s);
	for(i=0;s[i]!='\0';i++)
	{
	    if((s[i]>='0' )&& (s[i]<='9'))
	    {
	        sum+=(s[i]-'0');
	    }
	}
	printf("%d",sum);
	return 0;
}

